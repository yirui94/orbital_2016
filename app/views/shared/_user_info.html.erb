<%= link_to gravatar_for(current_user, size: 100), current_user %>
<h3><%= current_user.name %></h3>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.micropost.count, "micropost") %></span>