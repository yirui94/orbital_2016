<link rel="stylesheet" href="http://www.w3schools.com/lib/w3.css">
<link rel="stylesheet" href="http://cdnjs.cloudfare.com/ajax/libs/font-awesome/4.6.3/css/font-awesome.min.css">

<% provide(:title, 'All Artists') %>
<div class="container-fluid">
	<div class="row">
		<h1>Browse Artists</h1>

		<%= render 'search_form' %>
		<br>
	</div>
</div>
	<div class ="container-fluid">
	<h1> Our Artists</h1>
	<br>
	<% @users.each do |user| %>
		<ul class="w3-ul w3-card-4">
			<li class="w3-padding-16">
				<% if logged_in? && current_user.admin? && !current_user?(user) %>
				<button class="w3-btn w3-red w3-right w3-round -xxlarge"><%= link_to "delete", user, method: :delete, data: { confirm: "You sure?"} %></button>
				<% end %>
				<class="w3-left w3-circle w3-margin-right" style="width:60px"><%= gravatar_for user %>
				<span class="w3-xlarge"><%= link_to user.name, user %></span><br>
				<span> <%= ISO3166::Country.find_country_by_alpha2(user.user_detail.country).name %></span><br>
				<span> <%= user.user_detail.medium %> </span>
			</li>
		</ul>
	<% end %>
	<%= will_paginate %>
	</div>
	
<!-- 	<ul class = "users">
		<% @users.each do |user| %>
			<li>
				<%= gravatar_for user, size: 50 %>
				<%= link_to user.name, user %>
				<% if logged_in? && current_user.admin? && !current_user?(user) %>
					| <%= link_to "delete", user, method: :delete,
													data: { confirm: "You sure?"} %>
				<% end %>
			</li>
		<% end %>
	</ul>
	<%= will_paginate %>   -->